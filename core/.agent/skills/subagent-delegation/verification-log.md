# Subagent Delegation — Verification Log

> Adopters: record the resolved-driver live smoke and builder write-route probe here.
> A `[B]` is never a pass. Directory presence is NOT a detection signal — resolve the
> driver via `harness-profiles.md`, then prove the assistant-addressable worker route.

## Resolved Driver Probe

resolved_driver: <Cursor | Claude Code | none>
harness_version: <fill>
model_rung: <fill>
readonly_result: <pass | fail | pending>
probe_status: <passed | failed | pending>

**Resolution basis (not directory presence):** ...

**Verifier probe evidence:** receipt path under `{{RECEIPTS_DIR}}/`

**Builder write-route probe:** receipt path under `{{RECEIPTS_DIR}}/`

## Pending Probe (non-resolved harness)

harness: <other>
status: pending
