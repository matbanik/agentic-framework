# Known Issues — Archive

Resolved or dismissed issues move here (or to `status: resolved` in the YAML SSOT,
depending on your archive policy). Keep this file short; prefer YAML `status: resolved`
plus a one-line archive table if you outgrow a freeform file.
