# Session grouping proposals

`/session-grouping` writes proposals here as:

`{phase|YYYY-MM-DD}-{slug}-session-grouping.md`

Preferred input: `.agent/context/triage-output.yaml` (from `/issue-triage`).
Fallback: `.agent/context/known-issues.yaml`.
